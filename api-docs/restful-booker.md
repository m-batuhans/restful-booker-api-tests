# Restful-booker

API documentation for the playground API restful-booker. <a href='/' style='font-size: 24px'>Click here to go back to Home</a>

Source: https://restful-booker.herokuapp.com/apidoc/api_data.json and https://restful-booker.herokuapp.com/apidoc/api_project.json (generated by apidoc 0.25.0 on 2025-06-11T20:24:26.733Z)

## Auth

### Auth - CreateToken

Version: 1.0.0

Creates a new auth token to use for access to the PUT and DELETE /booking

**POST** `auth`

#### Header

**Header**

| Field | Type | Optional | Default value | Description |
|---|---|---|---|---|
| Content-Type | string | no | application/json | Sets the format of payload you are sending |

#### Parameters

**Request body**

| Field | Type | Optional | Default value | Description |
|---|---|---|---|---|
| username | String | no | admin | Username for authentication |
| password | String | no | password123 | Password for authentication |

#### Request examples

#### Example 1:

```json
curl -X POST \
  https://restful-booker.herokuapp.com/auth \
  -H 'Content-Type: application/json' \
  -d '{
    "username" : "admin",
    "password" : "password123"
}'
```

#### Success fields

**Success 200**

| Field | Type | Optional | Default value | Description |
|---|---|---|---|---|
| token | String | no |  | Token to use in future requests |

#### Success examples

#### Response:

```json
HTTP/1.1 200 OK

{
    "token": "abc123"
}
```

---

## Booking

### Booking - GetBookingIds

Version: 1.0.0

Returns the ids of all the bookings that exist within the API. Can take optional query strings to search and return a subset of booking ids.

**GET** `booking`

#### Parameters

**Parameter**

| Field | Type | Optional | Default value | Description |
|---|---|---|---|---|
| firstname | String | yes |  | Return bookings with a specific firstname |
| lastname | String | yes |  | Return bookings with a specific lastname |
| checkin | date | yes |  | Return bookings that have a checkin date greater than or equal to the set checkin date. Format must be CCYY-MM-DD |
| checkout | date | yes |  | Return bookings that have a checkout date greater than or equal to the set checkout date. Format must be CCYY-MM-DD |

#### Request examples

#### Example 1 (All IDs):

```json
curl -i https://restful-booker.herokuapp.com/booking
```

#### Example 2 (Filter by name):

```json
curl -i https://restful-booker.herokuapp.com/booking?firstname=sally&lastname=brown
```

#### Example 3 (Filter by checkin/checkout date):

```json
curl -i https://restful-booker.herokuapp.com/booking?checkin=2014-03-13&checkout=2014-05-21
```

#### Success fields

**Success 200**

| Field | Type | Optional | Default value | Description |
|---|---|---|---|---|
| object | object[] | no |  | Array of objects that contain unique booking IDs |
| object.bookingid | number | no |  | ID of a specific booking that matches search criteria |

#### Success examples

#### Response:

```json
HTTP/1.1 200 OK

[
  {
    "bookingid": 1
  },
  {
    "bookingid": 2
  },
  {
    "bookingid": 3
  },
  {
    "bookingid": 4
  }
]
```

---

### Booking - GetBooking

Version: 1.0.0

Returns a specific booking based upon the booking id provided

**GET** `booking/:id`

#### Header

**Header**

| Field | Type | Optional | Default value | Description |
|---|---|---|---|---|
| Accept | string | no | application/json | Sets what format the response body is returned in. Can be application/json or application/xml |

#### Parameters

**Url Parameter**

| Field | Type | Optional | Default value | Description |
|---|---|---|---|---|
| id | String | no |  | The id of the booking you would like to retrieve |

#### Request examples

#### Example 1 (Get booking):

```json
curl -i https://restful-booker.herokuapp.com/booking/1
```

#### Success fields

**Success 200**

| Field | Type | Optional | Default value | Description |
|---|---|---|---|---|
| firstname | String | no |  | Firstname for the guest who made the booking |
| lastname | String | no |  | Lastname for the guest who made the booking |
| totalprice | Number | no |  | The total price for the booking |
| depositpaid | Boolean | no |  | Whether the deposit has been paid or not |
| bookingdates | Object | no |  | Sub-object that contains the checkin and checkout dates |
| bookingdates.checkin | Date | no |  | Date the guest is checking in |
| bookingdates.checkout | Date | no |  | Date the guest is checking out |
| additionalneeds | String | no |  | Any other needs the guest has |

#### Success examples

#### JSON Response:

```json
HTTP/1.1 200 OK

{
    "firstname": "Sally",
    "lastname": "Brown",
    "totalprice": 111,
    "depositpaid": true,
    "bookingdates": {
        "checkin": "2013-02-23",
        "checkout": "2014-10-23"
    },
    "additionalneeds": "Breakfast"
}
```

#### XML Response:

```xml
HTTP/1.1 200 OK

<booking>
    <firstname>Sally</firstname>
    <lastname>Brown</lastname>
    <totalprice>111</totalprice>
    <depositpaid>true</depositpaid>
    <bookingdates>
        <checkin>2013-02-23</checkin>
        <checkout>2014-10-23</checkout>
    </bookingdates>
    <additionalneeds>Breakfast</additionalneeds>
</booking>
```

#### URL Response:

```url
HTTP/1.1 200 OK

firstname=Jim&lastname=Brown&totalprice=111&depositpaid=true&bookingdates%5Bcheckin%5D=2018-01-01&bookingdates%5Bcheckout%5D=2019-01-01
```

---

### Booking - CreateBooking

Version: 1.0.0

Creates a new booking in the API

**POST** `booking`

#### Header

**Header**

| Field | Type | Optional | Default value | Description |
|---|---|---|---|---|
| Content-Type | string | no | application/json | Sets the format of payload you are sending. Can be application/json or text/xml |
| Accept | string | no | application/json | Sets what format the response body is returned in. Can be application/json or application/xml |

#### Parameters

**Request body**

| Field | Type | Optional | Default value | Description |
|---|---|---|---|---|
| firstname | String | no |  | Firstname for the guest who made the booking |
| lastname | String | no |  | Lastname for the guest who made the booking |
| totalprice | Number | no |  | The total price for the booking |
| depositpaid | Boolean | no |  | Whether the deposit has been paid or not |
| bookingdates.checkin | Date | no |  | Date the guest is checking in |
| bookingdates.checkout | Date | no |  | Date the guest is checking out |
| additionalneeds | String | no |  | Any other needs the guest has |

#### Request examples

#### JSON example usage:

```json
curl -X POST \
  https://restful-booker.herokuapp.com/booking \
  -H 'Content-Type: application/json' \
  -d '{
    "firstname" : "Jim",
    "lastname" : "Brown",
    "totalprice" : 111,
    "depositpaid" : true,
    "bookingdates" : {
        "checkin" : "2018-01-01",
        "checkout" : "2019-01-01"
    },
    "additionalneeds" : "Breakfast"
}'
```

#### XML example usage:

```json
curl -X POST \
  https://restful-booker.herokuapp.com/booking \
  -H 'Content-Type: text/xml' \
  -d '<booking>
    <firstname>Jim</firstname>
    <lastname>Brown</lastname>
    <totalprice>111</totalprice>
    <depositpaid>true</depositpaid>
    <bookingdates>
      <checkin>2018-01-01</checkin>
      <checkout>2019-01-01</checkout>
    </bookingdates>
    <additionalneeds>Breakfast</additionalneeds>
  </booking>'
```

#### URLencoded example usage:

```json
curl -X POST \
  https://restful-booker.herokuapp.com/booking \
  -H 'Content-Type: application/x-www-form-urlencoded' \
  -d 'firstname=Jim&lastname=Brown&totalprice=111&depositpaid=true&bookingdates%5Bcheckin%5D=2018-01-01&bookingdates%5Bcheckout%5D=2018-01-02'
```

#### Success fields

**Success 200**

| Field | Type | Optional | Default value | Description |
|---|---|---|---|---|
| bookingid | Number | no |  | ID for newly created booking |
| booking | Object | no |  | Object that contains |
| booking.firstname | String | no |  | Firstname for the guest who made the booking |
| booking.lastname | String | no |  | Lastname for the guest who made the booking |
| booking.totalprice | Number | no |  | The total price for the booking |
| booking.depositpaid | Boolean | no |  | Whether the deposit has been paid or not |
| booking.bookingdates | Object | no |  | Sub-object that contains the checkin and checkout dates |
| booking.bookingdates.checkin | Date | no |  | Date the guest is checking in |
| booking.bookingdates.checkout | Date | no |  | Date the guest is checking out |
| booking.additionalneeds | String | no |  | Any other needs the guest has |

#### Success examples

#### JSON Response:

```json
HTTP/1.1 200 OK

{
    "bookingid": 1,
    "booking": {
        "firstname": "Jim",
        "lastname": "Brown",
        "totalprice": 111,
        "depositpaid": true,
        "bookingdates": {
            "checkin": "2018-01-01",
            "checkout": "2019-01-01"
        },
        "additionalneeds": "Breakfast"
    }
}
```

#### XML Response:

```xml
HTTP/1.1 200 OK

<?xml version='1.0'?>
<created-booking>
    <bookingid>1</bookingid>
    <booking>
        <firstname>Jim</firstname>
        <lastname>Brown</lastname>
        <totalprice>111</totalprice>
        <depositpaid>true</depositpaid>
        <bookingdates>
            <checkin>2018-01-01</checkin>
            <checkout>2019-01-01</checkout>
        </bookingdates>
        <additionalneeds>Breakfast</additionalneeds>
    </booking>
</created-booking>
```

#### URL Response:

```url
HTTP/1.1 200 OK

bookingid=1&booking%5Bfirstname%5D=Jim&booking%5Blastname%5D=Brown&booking%5Btotalprice%5D=111&booking%5Bdepositpaid%5D=true&booking%5Bbookingdates%5D%5Bcheckin%5D=2018-01-01&booking%5Bbookingdates%5D%5Bcheckout%5D=2019-01-01
```

---

### Booking - UpdateBooking

Version: 1.0.0

Updates a current booking

**PUT** `booking/:id`

#### Header

**Header**

| Field | Type | Optional | Default value | Description |
|---|---|---|---|---|
| Content-Type | string | no | application/json | Sets the format of payload you are sending. Can be application/json or text/xml |
| Accept | string | no | application/json | Sets what format the response body is returned in. Can be application/json or application/xml |
| Cookie | string | yes | token=&lt;token_value&gt; | Sets an authorization token to access the PUT endpoint, can be used as an alternative to the Authorization |
| Authorization | string | yes | Basic | YWRtaW46cGFzc3dvcmQxMjM=]   Basic authorization header to access the PUT endpoint, can be used as an alternative to the Cookie header |

#### Parameters

**Url Parameter**

| Field | Type | Optional | Default value | Description |
|---|---|---|---|---|
| id | Number | no |  | ID for the booking you want to update |

**Request body**

| Field | Type | Optional | Default value | Description |
|---|---|---|---|---|
| firstname | String | no |  | Firstname for the guest who made the booking |
| lastname | String | no |  | Lastname for the guest who made the booking |
| totalprice | Number | no |  | The total price for the booking |
| depositpaid | Boolean | no |  | Whether the deposit has been paid or not |
| bookingdates.checkin | Date | no |  | Date the guest is checking in |
| bookingdates.checkout | Date | no |  | Date the guest is checking out |
| additionalneeds | String | no |  | Any other needs the guest has |

#### Request examples

#### JSON example usage:

```json
curl -X PUT \
  https://restful-booker.herokuapp.com/booking/1 \
  -H 'Content-Type: application/json' \
  -H 'Accept: application/json' \
  -H 'Cookie: token=abc123' \
  -d '{
    "firstname" : "James",
    "lastname" : "Brown",
    "totalprice" : 111,
    "depositpaid" : true,
    "bookingdates" : {
        "checkin" : "2018-01-01",
        "checkout" : "2019-01-01"
    },
    "additionalneeds" : "Breakfast"
}'
```

#### XML example usage:

```json
curl -X PUT \
  https://restful-booker.herokuapp.com/booking/1 \
  -H 'Content-Type: text/xml' \
  -H 'Accept: application/xml' \
  -H 'Authorization: Basic YWRtaW46cGFzc3dvcmQxMjM=' \
  -d '<booking>
    <firstname>James</firstname>
    <lastname>Brown</lastname>
    <totalprice>111</totalprice>
    <depositpaid>true</depositpaid>
    <bookingdates>
      <checkin>2018-01-01</checkin>
      <checkout>2019-01-01</checkout>
    </bookingdates>
    <additionalneeds>Breakfast</additionalneeds>
  </booking>'
```

#### URLencoded example usage:

```json
curl -X PUT \
  https://restful-booker.herokuapp.com/booking/1 \
  -H 'Content-Type: application/x-www-form-urlencoded' \
  -H 'Accept: application/x-www-form-urlencoded' \
  -H 'Authorization: Basic YWRtaW46cGFzc3dvcmQxMjM=' \
  -d 'firstname=Jim&lastname=Brown&totalprice=111&depositpaid=true&bookingdates%5Bcheckin%5D=2018-01-01&bookingdates%5Bcheckout%5D=2018-01-02'
```

#### Success fields

**Success 200**

| Field | Type | Optional | Default value | Description |
|---|---|---|---|---|
| firstname | String | no |  | Firstname for the guest who made the booking |
| lastname | String | no |  | Lastname for the guest who made the booking |
| totalprice | Number | no |  | The total price for the booking |
| depositpaid | Boolean | no |  | Whether the deposit has been paid or not |
| bookingdates | Object | no |  | Sub-object that contains the checkin and checkout dates |
| bookingdates.checkin | Date | no |  | Date the guest is checking in |
| bookingdates.checkout | Date | no |  | Date the guest is checking out |
| additionalneeds | String | no |  | Any other needs the guest has |

#### Success examples

#### JSON Response:

```json
HTTP/1.1 200 OK

{
    "firstname" : "James",
    "lastname" : "Brown",
    "totalprice" : 111,
    "depositpaid" : true,
    "bookingdates" : {
        "checkin" : "2018-01-01",
        "checkout" : "2019-01-01"
    },
    "additionalneeds" : "Breakfast"
}
```

#### XML Response:

```xml
HTTP/1.1 200 OK

<booking>
    <firstname>James</firstname>
    <lastname>Brown</lastname>
    <totalprice>111</totalprice>
    <depositpaid>true</depositpaid>
    <bookingdates>
      <checkin>2018-01-01</checkin>
      <checkout>2019-01-01</checkout>
    </bookingdates>
    <additionalneeds>Breakfast</additionalneeds>
</booking>
```

#### URL Response:

```url
HTTP/1.1 200 OK

firstname=Jim&lastname=Brown&totalprice=111&depositpaid=true&bookingdates%5Bcheckin%5D=2018-01-01&bookingdates%5Bcheckout%5D=2019-01-01
```

---

### Booking - PartialUpdateBooking

Version: 1.0.0

Updates a current booking with a partial payload

**PATCH** `booking/:id`

#### Header

**Header**

| Field | Type | Optional | Default value | Description |
|---|---|---|---|---|
| Content-Type | string | no | application/json | Sets the format of payload you are sending. Can be application/json or text/xml |
| Accept | string | no | application/json | Sets what format the response body is returned in. Can be application/json or application/xml |
| Cookie | string | yes | token=&lt;token_value&gt; | Sets an authorization token to access the PUT endpoint, can be used as an alternative to the Authorization |
| Authorization | string | yes | Basic | YWRtaW46cGFzc3dvcmQxMjM=]   Basic authorization header to access the PUT endpoint, can be used as an alternative to the Cookie header |

#### Parameters

**Url Parameter**

| Field | Type | Optional | Default value | Description |
|---|---|---|---|---|
| id | Number | no |  | ID for the booking you want to update |

**Request body**

| Field | Type | Optional | Default value | Description |
|---|---|---|---|---|
| firstname | String | yes |  | Firstname for the guest who made the booking |
| lastname | String | yes |  | Lastname for the guest who made the booking |
| totalprice | Number | yes |  | The total price for the booking |
| depositpaid | Boolean | yes |  | Whether the deposit has been paid or not |
| bookingdates.checkin | Date | yes |  | Date the guest is checking in |
| bookingdates.checkout | Date | yes |  | Date the guest is checking out |
| additionalneeds | String | yes |  | Any other needs the guest has |

#### Request examples

#### JSON example usage:

```json
curl -X PUT \
  https://restful-booker.herokuapp.com/booking/1 \
  -H 'Content-Type: application/json' \
  -H 'Accept: application/json' \
  -H 'Cookie: token=abc123' \
  -d '{
    "firstname" : "James",
    "lastname" : "Brown"
}'
```

#### XML example usage:

```json
curl -X PUT \
  https://restful-booker.herokuapp.com/booking/1 \
  -H 'Content-Type: text/xml' \
  -H 'Accept: application/xml' \
  -H 'Authorization: Basic YWRtaW46cGFzc3dvcmQxMjM=' \
  -d '<booking>
    <firstname>James</firstname>
    <lastname>Brown</lastname>
  </booking>'
```

#### URLencoded example usage:

```json
curl -X PUT \
  https://restful-booker.herokuapp.com/booking/1 \
  -H 'Content-Type: application/x-www-form-urlencoded' \
  -H 'Accept: application/x-www-form-urlencoded' \
  -H 'Authorization: Basic YWRtaW46cGFzc3dvcmQxMjM=' \
  -d 'firstname=Jim&lastname=Brown'
```

#### Success fields

**Success 200**

| Field | Type | Optional | Default value | Description |
|---|---|---|---|---|
| firstname | String | no |  | Firstname for the guest who made the booking |
| lastname | String | no |  | Lastname for the guest who made the booking |
| totalprice | Number | no |  | The total price for the booking |
| depositpaid | Boolean | no |  | Whether the deposit has been paid or not |
| bookingdates | Object | no |  | Sub-object that contains the checkin and checkout dates |
| bookingdates.checkin | Date | no |  | Date the guest is checking in |
| bookingdates.checkout | Date | no |  | Date the guest is checking out |
| additionalneeds | String | no |  | Any other needs the guest has |

#### Success examples

#### JSON Response:

```json
HTTP/1.1 200 OK

{
    "firstname" : "James",
    "lastname" : "Brown",
    "totalprice" : 111,
    "depositpaid" : true,
    "bookingdates" : {
        "checkin" : "2018-01-01",
        "checkout" : "2019-01-01"
    },
    "additionalneeds" : "Breakfast"
}
```

#### XML Response:

```xml
HTTP/1.1 200 OK

<booking>
    <firstname>James</firstname>
    <lastname>Brown</lastname>
    <totalprice>111</totalprice>
    <depositpaid>true</depositpaid>
    <bookingdates>
      <checkin>2018-01-01</checkin>
      <checkout>2019-01-01</checkout>
    </bookingdates>
    <additionalneeds>Breakfast</additionalneeds>
</booking>
```

#### URL Response:

```url
HTTP/1.1 200 OK

firstname=Jim&lastname=Brown&totalprice=111&depositpaid=true&bookingdates%5Bcheckin%5D=2018-01-01&bookingdates%5Bcheckout%5D=2019-01-01
```

---

### Booking - DeleteBooking

Version: 1.0.0

Deletes a booking from the API. Requires an authorization token to be set in the header or a Basic auth header.

**DELETE** `booking/1`

#### Header

**Header**

| Field | Type | Optional | Default value | Description |
|---|---|---|---|---|
| Cookie | string | yes | token=&lt;token_value&gt; | Sets an authorization token to access the DELETE endpoint, can be used as an alternative to the Authorization |
| Authorization | string | yes | Basic | YWRtaW46cGFzc3dvcmQxMjM=]   Basic authorization header to access the DELETE endpoint, can be used as an alternative to the Cookie header |

#### Parameters

**Url Parameter**

| Field | Type | Optional | Default value | Description |
|---|---|---|---|---|
| id | Number | no |  | ID for the booking you want to update |

#### Request examples

#### Example 1 (Cookie):

```json
curl -X DELETE \
  https://restful-booker.herokuapp.com/booking/1 \
  -H 'Content-Type: application/json' \
  -H 'Cookie: token=abc123'
```

#### Example 2 (Basic auth):

```json
curl -X DELETE \
  https://restful-booker.herokuapp.com/booking/1 \
  -H 'Content-Type: application/json' \
  -H 'Authorization: Basic YWRtaW46cGFzc3dvcmQxMjM='
```

#### Success fields

**Success 200**

| Field | Type | Optional | Default value | Description |
|---|---|---|---|---|
| OK | String | no |  | Default HTTP 201 response |

#### Success examples

#### Response:

```json
HTTP/1.1 201 Created
```

---

## Ping

### Ping - HealthCheck

Version: 1.0.0

A simple health check endpoint to confirm whether the API is up and running.

**GET** `ping`

#### Request examples

#### Ping server:

```json
curl -i https://restful-booker.herokuapp.com/ping
```

#### Success fields

**Success 200**

| Field | Type | Optional | Default value | Description |
|---|---|---|---|---|
| OK | String | no |  | Default HTTP 201 response |

#### Success examples

#### Response:

```json
HTTP/1.1 201 Created
```

---
